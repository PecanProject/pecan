##' .. content for \description{} (no empty lines) ..
##'
##' .. content for \details{} .. starts one run for one site with one set of parameter settings. Minimalistic function for starting a single run. called once for each run.
##' @title 
##' @param model.settings
##' @param pecan.settings
##' @param config.file  from file.name from write.config()
##' @param site 
##' ##' @return runs
##' @author David
start.run.ed <- function(model.settings, pecan.settings, config.file, site) {
# move scripts to remote host

# call write.config
  write.config(list, filename, configheader))
# run script on remote machine

# figure out when runs are finished
}
