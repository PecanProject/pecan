% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/load_posteriors.R
\name{load.posteriors}
\alias{load.posteriors}
\title{Load posterior distributions and/or Monte Carlo samples for a PFT}
\usage{
load.posteriors(
  posterior.file,
  outdir = NULL,
  posteriorid = NULL,
  con = NULL,
  hostname = NULL
)
}
\arguments{
\item{posterior.file}{path to a posterior file or directory, or \code{NA}.}

\item{outdir}{\strong{Deprecated.} Legacy PFT output directory used only
when \code{posterior.file} is \code{NA}. Will be removed in a future
version; pass an explicit \code{posterior.file} instead.}

\item{posteriorid}{\strong{Deprecated.} Posterior ID for DB lookup, used
only in the legacy fallback path.}

\item{con}{\strong{Deprecated.} Database connection, used only in the
legacy fallback path.}

\item{hostname}{\strong{Deprecated.} Hostname for DB file lookup, used
only in the legacy fallback path.}
}
\value{
list with \code{prior.distns}, \code{trait.mcmc}, \code{is.joint}.
  \code{is.joint} is \code{TRUE} when MCMC samples represent a joint
  posterior (e.g. from PDA) and parameter correlations should be preserved.
}
\description{
Detects posterior type by file contents, not filenames.
Monte Carlo samples (\code{trait.mcmc}) take precedence over
distribution summaries (\code{post.distns} / \code{prior.distns}).
Falls back to legacy \code{outdir} + DB lookup when
\code{posterior.file} is \code{NA}, with a deprecation warning.
}
\author{
Siddhey Patil
}
\keyword{internal}
