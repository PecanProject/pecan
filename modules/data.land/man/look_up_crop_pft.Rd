% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/look_up_crop_pft.R
\name{look_up_crop_pft}
\alias{look_up_crop_pft}
\title{Look up PFT assignments by LandIQ crop code}
\usage{
look_up_crop_pft(
  crop_type,
  crop_code = NA_character_,
  output = c("all", "pft_group", "pecan_pft")
)
}
\arguments{
\item{crop_type}{Character vector of LandIQ CLASS codes.}

\item{crop_code}{Character vector of LandIQ SUBCLASS codes, same length as
`crop_type` or length one. Use NA for class-only lookup.}

\item{output}{One of "all" (default), "pft_group", "pecan_pft".}
}
\value{
A tibble with one row per input. Columns: `crop_type`, `crop_code`,
  `crop_desc`, and `pft_group` and/or `pecan_pft` depending on `output`.
}
\description{
Returns PFT assignments for LandIQ CLASS/SUBCLASS codes.
}
\examples{
look_up_crop_pft("D", "12")
look_up_crop_pft(c("D", "G", "R"), c("12", "2", "1"))
look_up_crop_pft("F", "6", output = "pecan_pft")

}
