# PEcAn RTM -- Makevars
# Author: Alexey Shiklomanov, Dept of Earth and Environment / Boston University
# Email: ashiklom@bu.edu

OBJECTS = RTM/rtm.o

all : $(SHLIB)

$(SHLIB): RTM/rtm.o

RTM/rtm.o:
	@(cd RTM && \
	($(MAKE) all FC="$(FC)"))

clean :
	rm *.o *.so *.mod
