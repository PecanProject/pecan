% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run.meta.analysis.R
\name{meta_analysis_standalone}
\alias{meta_analysis_standalone}
\title{Complete meta-analysis workflow for a single plant functional type (PFT)}
\usage{
meta_analysis_standalone(
  trait_data,
  priors,
  iterations,
  outdir = file.path(tempdir(), "pecan-meta-analysis"),
  pft_name = NA_character_,
  random = TRUE,
  threshold = 1.2,
  use_ghs = TRUE,
  prior_n = 0.02,
  prior_cv = sqrt(0.001),
  tauA = NULL,
  tauB = NULL
)
}
\arguments{
\item{trait_data}{(list) Named list of trait data. List item names must be
trait names (consistent with \code{priors} argument). List values are
\code{data.frame}s with the following required columns:
\code{name}, \code{mean} \code{statname}, \code{stat}, \code{greenhouse}, \code{n},
\code{site_id}, \code{specie_id}, \code{citation_id}, \code{cultivar_id},
\code{date}, \code{time}, \code{control}}

\item{priors}{(list) Named list of priors}

\item{iterations}{(integer) Number of sampler iterations for MCMC analysis}

\item{outdir}{(character; default = \code{tempdir() / "pecan-meta-analysis"})
Path to directory where outputs will be stored.}

\item{pft_name}{(character; default = NA) Name of PFT (for logging purposes).}

\item{random}{(boolean; default = TRUE) Should random effects be used?}

\item{threshold}{Gelman-Rubin convergence diagnostic (MGPRF)
default = 1.2 following Bolker 2008 Ecological Models and Data in R}

\item{use_ghs}{(boolean; default = TRUE) If TRUE, do not exclude greenhouse data}

\item{prior_n}{(numeric; default = 0.02) Prior effective sample size (n0),
where \code{tauA = prior_n / 2}. Ignored if \code{tauA} is provided directly.}

\item{prior_cv}{(numeric; default = \code{sqrt(0.001)}) Prior coefficient of
variation (SD / mean), used to set \code{prior_variance = prior_cv^2 * mean^2}.
Ignored if \code{tauB} is provided directly.}

\item{tauA}{(numeric or NULL; default = NULL) Direct override for the first
Gamma prior parameter (\code{tauA = n0 / 2}). If NULL, derived from \code{prior_n}.}

\item{tauB}{(numeric or NULL; default = NULL) Direct override for the second
Gamma prior parameter (\code{tauB = tauA * prior_variance}). If NULL, derived
from \code{prior_n} and \code{prior_cv}. Must be a named numeric vector with names
matching trait names in \code{trait_data}.}
}
\value{
(list) List of trait meta-analysis results, including:
\itemize{
\item \code{trait.mcmc}: MCMC samples
\item \code{post.distns}: Posterior distributions
\item \code{jagged.data}: "JAGS-ified" input data (after GHG screen, if applied)
}
}
\description{
Complete meta-analysis workflow for a single plant functional type (PFT)
}
