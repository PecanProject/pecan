% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/adjust.biomass.LPJGUESS.R
\name{safe_scale_N}
\alias{safe_scale_N}
\title{Safely scale an N pool by carbon change}
\usage{
safe_scale_N(n_old, C_old, C_new)
}
\arguments{
\item{n_old}{Numeric scalar. Previous N mass (per individual).}

\item{C_old}{Numeric scalar. Previous C mass (per individual).}

\item{C_new}{Numeric scalar. Updated C mass (per individual).}
}
\value{
Numeric scalar. A finite, non-negative N mass. Returns `0` when a
        valid scale cannot be computed.
}
\description{
Compute \eqn{n_\mathrm{new} = n_\mathrm{old} \times (C_\mathrm{new}/C_\mathrm{old})}
with guards against division-by-zero and non-finite values.
If `C_old <= 0` or not finite, returns `0`. If the scale factor
`C_new / C_old` is not finite or `<= 0`, also returns `0`.
This is used in \code{adjust.biomass.LPJGUESS()} to preserve C:N ratios
without introducing \code{NaN}/\code{Inf} when pools are empty (e.g., heartwood).
}
\keyword{internal}
